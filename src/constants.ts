export enum ConnectorState {
  Connecting = "CONNECTING",
  Connected = "CONNECTED",
  Disconnected = "DISCONNECTED",
}

export enum WindowMessageTypes {
  ProviderSuccess = "ETHEREUM_PROVIDER_SUCCESS",
  ProviderRequest = "ETHEREUM_PROVIDER_REQUEST",
}
